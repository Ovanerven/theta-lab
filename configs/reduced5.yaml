dataset_path: datasets/N1000_T300_steps600_zeros_knoise0.0_reduced5_ADGJM.npz
exp_name: 5eq_20ep_lr1e-3_substeps1_thetahi2.0lo1e-3_no0clamp

epochs: 20
batch_size: 1000
lr: 0.001
weight_decay: 0.0085
val_frac: 0.15
seed: 42

hidden: 128
num_layers: 1
grad_clip: 1.0

teacher_forcing: true
tf_every: 50
tf_drop_epoch: 250

scaffold: reduced5

theta_lo: 1.0e-3
theta_hi: 2.0

n_substeps: 1

study: reduced5_study

ckpt_every: 2    # save checkpoint every 5 epochs (0 = disable)